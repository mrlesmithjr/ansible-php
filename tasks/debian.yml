---
- name: debian | Updating APT Cache
  apt:
    update_cache: true
  become: true

- name: debian | Installing PHP Packages
  apt:
    name: "{{ php_debian_packages }}"
    state: present
  become: true
